export interface Iproduct {
    id: number;
    title: string;
    price: number;
    alt: string;
    image: string;
}

export interface ICard {
    id: number;
    title: string,
    price:number,
    image: string,
    alt: string,
    // onClick: () => void,
    // inCart: boolean,
    // count: number,
}